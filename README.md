# Waitlist Bot ( BETA )

A powerful, customizable Discord waitlist management bot with role protection, automatic updates, server-specific storage, logging, renaming support, and interactive buttons.
Includes a built-in 24/7 status webpage compatible with Pella, Koyeb, and other cloud hosts.

✨ Features

📝 Create & manage unlimited waitlists

➕ Add/Remove users with permission checks

🔄 Update button (anyone can press)

🗑 Delete button (manager/admin only)

✏️ Rename waitlists — automatically updates posted messages

📁 Per-server storage (no cross-server mixing)

🧭 Autocomplete waitlist names

🛡 /setup system

Select multiple manager roles

Select waitlist posting channel

Select log channel

📝 Logging

Add logs

Remove logs

Delete logs

Rename logs

All include Discord timestamps

🧪 Commands List
⚙️ Setup

/setup
Select:

Manager roles

Waitlist channel

Log channel

📌 Waitlist Management

/createwaitlist name:
→ Creates a waitlist.

/deletewaitlist waitlist:
→ Deletes it.

/renamewaitlist old new
→ Renames + updates posted buttons.

/purgeallwaitlists (Admin only)
→ Deletes ALL waitlists for the server.

👤 User Controls

/add user: waitlist:
→ Adds user.

/remove user: waitlist:
→ Removes user.

📤 Posting

/setwaitlist waitlist:
→ Posts an embed with:

Update button

Delete button

📄 Info Commands

/listwaitlists
→ Shows all waitlists + users in each.

/cmds
→ Lists all commands.

/ping
→ Bot version + latency.
